"""Keelspring — the game-agnostic agent-sim engine (docs/KEELSPRING.md).

The keel a game is laid down on; the mainspring that drives it. Flotilla is
the first game built on it (registration: sim/run_config.py).

Grows module-by-module through the split stages; tests/test_engine_boundary.py
enforces that nothing here imports the game."""
